// this should be overridden by the one generated in the "shared" directory,
// but this fallback version is here just in case (to prevent compiler errors)
#define SRCVERSION 90
#define SRCVERSIONSTRING "r90"
